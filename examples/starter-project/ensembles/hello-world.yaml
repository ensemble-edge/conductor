name: hello-world
description: Basic ensemble example demonstrating Conductor's capabilities

flow:
  - member: greet
    input:
      name: ${input.name}

output:
  greeting: ${greet.output.message}
