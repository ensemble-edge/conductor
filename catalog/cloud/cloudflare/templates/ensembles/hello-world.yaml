name: hello-world
description: |
  Basic ensemble example demonstrating Conductor's capabilities:
  - Uses shared prompts (prompts/greeting.md)
  - Uses shared configs (configs/greet-settings.yaml)
  - Shows how members can reference versioned components

flow:
  - member: greet
    input:
      name: ${input.name}
      style: ${input.style || "friendly"}
      language: ${input.language || "en"}

output:
  greeting: ${greet.output.message}
  metadata:
    promptUsed: "prompts/greeting.md"
    configUsed: "configs/greet-settings.yaml"
