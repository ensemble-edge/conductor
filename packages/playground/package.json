{
  "name": "@conductor/playground",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Development playground for testing Conductor and plugins",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'Playground does not build'",
    "test": "echo 'Playground has no tests'"
  },
  "dependencies": {
    "@ensemble-edge/conductor": "workspace:*",
    "@ensemble-edge/plugin-unkey": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
