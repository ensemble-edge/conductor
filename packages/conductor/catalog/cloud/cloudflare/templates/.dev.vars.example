# ============================================================
# Local Development Secrets
# ============================================================
# 🚨 FIRST TIME SETUP:
#    1. Copy this file: cp .dev.vars.example .dev.vars
#    2. Add your secrets to .dev.vars (it's in .gitignore)
#    3. Run: wrangler dev
#
# This file (.dev.vars) is automatically loaded by `wrangler dev`
# ============================================================

# ==================== Workers AI (Recommended) ====================
# For Think agents using Cloudflare's Workers AI
#
# 📋 Setup Steps:
# 1. Go to: dash.cloudflare.com → My Profile → API Tokens
# 2. Click "Create Token"
# 3. Use "Edit Cloudflare Workers" template OR create custom with:
#    - Account → Workers AI → Edit
# 4. Copy the token below (starts with letters/numbers, not "Bearer")
#
# CLOUDFLARE_API_TOKEN=your-token-here
#
# Then run: CLOUDFLARE_API_TOKEN="your-token" wrangler dev
# Or add to your shell: export CLOUDFLARE_API_TOKEN="your-token"

# ==================== External AI Providers ====================
# For Think agents using Anthropic or OpenAI directly
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# ==================== Other Secrets ====================
# Add any other secrets your application needs
# MY_API_KEY=
# DATABASE_URL=

# ============================================================
# USAGE GUIDE:
# ============================================================
# LOCAL DEVELOPMENT:
#   1. Copy .dev.vars.example to .dev.vars
#   2. Uncomment and add your values above
#   3. Run: wrangler dev (or with token: CLOUDFLARE_API_TOKEN="..." wrangler dev)
#   4. Access in code: env.ANTHROPIC_API_KEY
#
# PRODUCTION DEPLOYMENT:
#   1. Set secrets in Cloudflare (not in .dev.vars)
#   2. Run: wrangler secret put ANTHROPIC_API_KEY
#   3. Paste your key when prompted
#   4. Repeat for each secret
#
# WORKERS AI TROUBLESHOOTING:
#   - "Authentication error" → Token missing Workers AI permission
#   - "AI remote" in logs → Normal for dev mode (AI uses remote API)
#   - Empty {} response → Check ensemble output mapping syntax
#
# Learn more: https://developers.cloudflare.com/workers/configuration/secrets/
# ============================================================
