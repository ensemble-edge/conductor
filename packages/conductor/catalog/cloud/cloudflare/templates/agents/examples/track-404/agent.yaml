name: track-404
operation: code
description: Tracks 404 errors in D1 for analytics

schema:
  input:
    requestedPath: string?
    referrer: string?
    userAgent: string?
  output:
    tracked: boolean
