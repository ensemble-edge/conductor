name: process-middleware-request
operation: code
description: Demonstrates middleware functionality by returning request metadata

schema:
  input: {}
  output:
    message: string
    method: string
    path: string
    timestamp: string
    middlewareApplied: string[]
